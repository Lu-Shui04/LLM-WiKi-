# SCHEMA.md — LLM Wiki 页面契约

> 这份文件是 Wiki 的**唯一权威规范**。人、编译器、lint 都读它。
>
> 编译器在每次编译时把它整篇发给 LLM。它的内容参与 `compiler_fp` 指纹计算，
> 所以**改动这个文件会让下一次 ingest 全量重编**（3–8 分钟、数万 token）。
> 这是期望行为，不是 bug——规范变了，产物就该跟着变。

---

## 〇、两层结构

```
knowledge/   原始素材层。只读、不可变。一切事实的最终溯源地。
wiki/        编译产物层。LLM 写页面内容，代码写索引和日志。
```

`wiki/` 是**纯派生物**：删掉它下面除 `SCHEMA.md` 之外的一切，再重跑 ingest，能得到等价结果。
所以 wiki 坏了不用修，删掉重跑即可。反过来，任何只存在于 wiki 而不在 knowledge 里的事实，
都是编译过程编出来的，属于严重错误。

---

## 一、页面类型

| type | 存放目录 | 用途 | 何时创建 |
|---|---|---|---|
| `summary` | `summaries/` | 一份原始文档的摘要页 | 每份原始文档一个，一一对应，必然存在 |
| `entity` | `entities/` | 人物、组织、产品、项目等具体名词 | 素材中出现且值得单独成页 |
| `concept` | `concepts/` | 方法、技能、技术主题、流程 | 跨文档复用的知识点 |
| `comparison` | `comparisons/` | 两个及以上事物的对比 | 按需。素材里真的有对比时才建，不要为了凑类型硬造 |
| `query` | `queries/` | 高价值问答归档 | 按需。用户在 query 时显式存档才建 |
| `overview` | 根目录 `overview.md` | 知识库总览 | 全局唯一一个 |

**宁少勿多。** 页面不是越多越显得完善。一个概念如果只在某一份文档里出现过一次、
也没有交叉引用的价值，就留在摘要页里，不要单独开页。
判断标准：**假设某个问题会问到它，删掉这页会让答案缺一块——那才值得单开。**

## 二、目录结构

```
wiki/
  index.md          全局索引（代码渲染，不从 LLM 出）
  log.md            追加式操作日志（代码追加）
  overview.md       知识库总览
  summaries/        来源摘要页
  entities/         实体页
  concepts/         概念页
  comparisons/      对比页（按需）
  queries/          问答归档（按需）
  .cache/           分析缓存与 LLM 原始输出（不进版本库）
```

---

## 三、YAML frontmatter

### LLM 必须写的字段

```yaml
---
title: 意图路由
type: concept
summary: 用规则显式分流用户意图，绕开向量检索不可靠的问题
tags: [速购客服, 架构]
contradictions: none
---
```

| 字段 | 约束 |
|---|---|
| `title` | 显示名。**允许含中文全角冒号**（解析按第一个半角 `:` 切分，全角 `：` 天然安全） |
| `type` | 必须是第一节表格里的六种之一 |
| `summary` | **一句话**，会被代码抄进 `index.md`。不写摘要的页面等于从索引里消失 |
| `tags` | 行内列表 `[a, b]` 或块列表均可，2–5 个 |
| `contradictions` | `none` / `open` / `resolved` 三选一，见第六节 |

### 代码自动维护的字段（LLM 不要写）

```yaml
sources: [我的简历.md]
created: 2026-09-13
updated: 2026-09-13
```

| 字段 | 说明 |
|---|---|
| `sources` | 相对 `knowledge/` 的文件名列表。**由代码合并**：本次来源并进已有列表，不覆盖 |
| `created` | 首次创建日期，`YYYY-MM-DD` 字符串，**永不变** |
| `updated` | 最后修改日期，每次真正写盘才更新 |

只有 `summaries/` 页额外带两个字段，同样由代码写：

```yaml
source_sha: 3f2a91c4      # 源文档内容的 sha256 前 8 位
compiler_fp: b7e10d55     # 编译提示词 + compiler.py + SCHEMA.md 的指纹
```

两者都与当前值一致时，整个文档**跳过编译，零 token**。

> **不支持嵌套 map、不支持多行块标量。** 只支持扁平 key-value、行内列表和块列表。
> 这是刻意的：解析器是手写的确定性小函数，能力边界清晰，行为可单测。

---

## 四、页面固定结构

```markdown
## 摘要

两三句话。比 frontmatter 的 summary 详细，比正文简略。

## 要点

- 一条一个独立事实，条目式
- 能被单独引用

## 细节

### 来自《我的简历》

（这份来源提供的展开内容）

### 来自《常见问题回答》

（同上）

## 相关

- [[意图路由]]
- [[RAG切块策略]]
```

**五段结构里 `## 矛盾` 是条件段**——没有矛盾就整节省略，**不要写"无"**、不要留空小节。
有矛盾时按第六节的格式追加在 `## 相关` 之后。

`## 细节` 下**按来源分 ### 小节**，标题固定为 `### 来自《文件名》`。
新来源的内容追加为新的 ### 小节，**不动已有小节**。

### 基本信息必须逐字照抄（含联系方式）

简历类来源的「基本信息」（姓名、性别、学历、毕业时间、**电话、邮箱**）
必须**原样保留**在摘要页里，**不许替换成「见原文」「略」之类的指针**。

这一条是踩过坑写下来的：模型会按自己的隐私先验把手机号换成「电话与邮箱见原文」——
出发点是好意，但结果是一页答不了这个问题。**联系方式不是要脱敏的对象，
而是这份知识库本来就要能回答的内容之一**：有人问「你的联系方式」，
答案必须能从 Wiki 页面上直接读出来，而 `knowledge/` 里的原文不是给提问者看的。

同理适用于：证件与证书名称、学校与专业名称、公司或项目专有名称、具体数字。
**专有名词和事实性字段一律照抄，不做任何「善意加工」。**

---

## 五、命名规范

文件名 = 中文 slug。规则全部是确定性变换，无 LLM 参与：

1. **NFC 规范化**（把分解式的组合字符统一成合成式）
2. 去首尾空白
3. **剔除 Windows 非法字符**：`\ / : * ? " < > |` 及 ASCII 控制字符
4. 空格、全角空格 → `-`
5. 折叠连续 `-` 为一个
6. 去首尾的 `-` 和 `.`
7. **截断 60 字符**
8. 规范化后为空 → 兜底 `page-<sha256 前 8 位>`（lint 会提示人工指定）

**唯一性必须大小写不敏感地判定**——NTFS 不区分大小写，
`RAG切块策略.md` 和 `rag切块策略.md` 在磁盘上是同一个文件。

文件名可以用中文（`concepts/意图路由.md`），因为产物是给人看、给人翻的。

---

## 六、矛盾标记

### 什么算矛盾

**同一事实被两处给出不相容的陈述。**

### 什么不算矛盾（务必不要报）

1. **跨文档的技术栈差异，若分属不同项目或语境。**
   《核心技能》里的 pgvector 是**技能清单**（在别处用过），
   本项目用 SQLite + numpy 是**具体实现**——两者不冲突。
2. **颗粒度差异。**
   FAQ 说「chunkSize 500 是试出来的」vs 简历说「4 是反复试出来的平衡点」，
   这是详略之分，不是打架。
3. **通用描述 vs 具体实例。**
   「掌握 RAG 全流程」与「本项目检索走内存点积」，是抽象与具体的关系。

跨文档差异的正确归属是 `## 相关` 或"知识空白"小节，**不是矛盾**。
把详略差异报成矛盾，会让产物充满噪声警告，等于把真矛盾也淹没了。

### 强制三件套

报一条矛盾，必须**同时**给出：

```
> [!WARNING] 矛盾
> **A**（我的简历.md「项目经历 / 三、RAG」）：原文摘录，原样引号引用
> **B**（常见问题回答.md「3.」）：原文摘录，原样引号引用
> **为何不相容**：一句话说明两者为何无法同时成立
```

**找不到两段真正打架的原文，就不许报。** 编不出 B 处摘录，说明这不矛盾。

### frontmatter 同步

- 报了矛盾 → `contradictions: open`
- 矛盾已在后续来源中消解 → `contradictions: resolved`
- 无矛盾 → `contradictions: none`

lint 只报 `open` 的，以及"正文有 `> [!WARNING] 矛盾` 块但 frontmatter 写着 `none`"的不一致。

---

## 七、交叉引用

- 语法统一为 `[[中文标题]]`，**不带目录前缀**。
  目录结构变了引用不会作废，是这么设计的理由。
- `[[X]]` 的解析顺序：① `title` 等于 X 的页面 → ② 文件名 slug 等于 X 的页面 →
  ③ 本次编译将要创建的页面。
- **目标只能从编译第一步产出的页面清单里选。** 不许凭空引用没列出来的页。
- 链接集合减去已存在页面、减去本次将创建的页面 = **死链集合**。
  死链**降级为纯文本**（去掉 `[[ ]]` 保留文字）并记进 `log.md`，
  而不是留下悬空的 `[[不存在的页]]`——产物是给人看的，点不开的引用是纯噪声。
- 确实需要留悬空引用时用 `--allow-dangling`，默认关闭。

---

## 八、`op` 决策规则（幂等）

编译第一步会给每个页面分配一个操作：

| op | 含义 | 落盘行为 |
|---|---|---|
| `create` | 这是新页 | 全新写入 |
| `update` | 已有页，本次来源带来了新事实 | **整页重写** |
| `skip` | 已有页，本次来源没带来新东西 | **完全不写盘** |

**`op` 由 LLM 显式给出，代码只执行不猜。** 把"新建还是更新"变成显式输出，
是幂等的第一性原理——不猜就不会猜错。

代码侧兜底：

- `op: create` 但目标 slug 已存在 → **自动降级为 `update`**，并往 `log.md` 记一条 warning。
  这挡住"模型每次造一个略不同的 slug 导致同主题页面分裂"。

### 更新 = 整页重写（铁律）

**不是补丁，不是追加。** 重新通读该页现有全文 + 本次来源，产出完整的新版本，整文件覆盖。
这和处理原生文档时"整篇覆盖"的做法同构。

由此推出：**页面长度由内容决定，不由 ingest 次数决定。**
同一份文档 ingest 十次，页面不会膨胀十倍。膨胀问题从根上消失。

### 反 churn 条款

**禁止为了改写而改写。** 除非新来源确实包含该页**还没有的事实**，
否则输出 `op: skip`，一个字都不写。

同义改写、调整措辞、重新排版、把两句话合成一句——这些都不是"新事实"。
模型天然倾向于"既然读了就产出点什么"，这条是唯一有效的对抗手段。

### 增量跳过（整份文档级）

`summaries/` 页的 `source_sha` 与 `compiler_fp` 双双匹配当前值时，
整份文档跳过编译，不发起任何 LLM 调用，用时 < 1 秒。

---

## 九、`log.md` 格式

由代码追加，**LLM 不写**。每次 ingest 追加一段：

```markdown
## 2026-09-13 15:04 ingest 我的简历.md

- 模型：deepseek-v4-pro
- 用量：输入 28,431 / 输出 12,067 tokens
- 新建：summaries/我的简历.md、concepts/意图路由.md、entities/速购AI客服系统.md
- 更新：（无）
- 跳过：concepts/RAG切块策略.md（op: skip）
- 死链降级：[[订单Agent]] → 订单Agent
- 告警：无

<details><summary>编译推理（草稿，非页面内容）</summary>

（模型第一、二步的 reasoning_content 原文）

</details>
```

推理内容进 `<details>` 折叠块，**不进页面**——页面是给人看的成品，推理是过程草稿。
但调假矛盾规则时，推理是唯一能看到模型"为什么把这条判成矛盾"的地方，价值很高。
`--no-reasoning` 可关。

---

## 十、工作流

### ingest（摄入一份文档）

```
1. 算 source_sha 与 compiler_fp；与 summaries/ 页记录比对，一致则整份跳过
2. LLM 第一步·分析（1 次调用）
   输入：源文档全文 + 本文件 + 现有 index.md + 各页一级标题大纲
   输出：短结构 JSON —— 每页一条 {slug, type, op, title, summary, sources, existing}
        + 矛盾清单 + 知识空白
3. LLM 第二步·生成（3 次调用，按页面组拆：摘要页 / 实体+概念页 / overview）
   输出：===FILE: <路径>=== 分隔符协议的多文件文本
4. 纯代码校验：路径白名单、frontmatter 必填字段、死链降级、
   op:create 撞已有 slug 降级为 update
5. 全部校验通过才统一落盘（两阶段提交）；任一步失败整次 ingest 不写盘
6. 代码渲染 index.md、追加 log.md
```

**为什么正文不用 JSON 输出**：中文 Markdown 嵌进 JSON，换行和引号必须转义，
模型漏一处，`json.loads` 是**全有或全无**——一次编译全废。
分隔符协议是行级容错的，坏一段只坏一段，还能原样落盘。

**为什么分组调用**：单次输出太长，模型后半段会敷衍；且一处被 `max_tokens` 截断要整组重来。
每组控制在 4000 字以内。

### query（问答）

```
1. 读取 index.md 与全部页面
2. 页面数 ≤ wiki_inline_max_pages 时：全量进上下文，不做选页
   超过时：代码按 title + tags + 摘要做字符 n-gram 打分取 top 8（不用 LLM、不用向量）
3. LLM 基于给定页面作答，每个事实标注 [[来源页]]
4. 页面里没有的，明说"Wiki 里没有"，不用通用知识补
```

**为什么默认不做 LLM 选页**：选页恰恰是又一次"判断准不准"的问题。
全量注入省掉一次往返，跨页问题也不会因选页失误缺一半料。
`--pages` 可手工指定兜底。

### lint（静态检查）

六类检查，全是确定性代码，不调 LLM：

| 检查 | 判据 |
|---|---|
| 孤立页面 | 没有任何页面链向它（`summaries/`、`overview.md`、`index.md` 除外） |
| 死链 | `[[X]]` 找不到对应页面 |
| 重复实体/概念 | 同目录下 title 或 slug 相似度超过阈值（`difflib.SequenceMatcher`） |
| 无来源断言 | 有 `## 要点` 但 frontmatter `sources` 为空 |
| 未处理矛盾 | `contradictions: open`，或正文有 `> [!WARNING] 矛盾` 但 frontmatter 是 `none` |
| frontmatter 缺失 | 缺 `title` / `type` / `summary` 等必填字段 |

另有 warn 级：单页正文超过 2000 字（可能该拆分或该精简）。
存在 error 时退出码为 1。

---

## 十一、未来：FTS5（现在不实现）

如果 Wiki 页面超过 **50** 个，全量注入上下文会开始不划算。
届时的方案是 SQLite FTS5 + BM25 建关键词索引，加一个 `reindex` 命令，
用 BM25 分数替代现在的字符 n-gram 打分，**仍然是确定性的，仍然不引入 embedding**。

现在不要实现这一层。当前规模（< 15 页）全量读取更简单也更快。

---

## 十二、红线

1. **`knowledge/` 只读。** 不修改、不移动、不重命名、不删除。
2. **不许无来源断言。** wiki 里出现的每个事实都必须能追溯到 `knowledge/` 的某份文档。
3. **不许留悬空 `[[ ]]`。** 死链一律降级为纯文本。
4. **不许为改写而改写。** 没有新事实就 `op: skip`。
5. **更新是整页重写，不是追加。** 页面长度由内容决定，不由 ingest 次数决定。
6. **报矛盾必须给出 A、B 两处原文摘录 + 不相容理由。** 凑不出就别报。
