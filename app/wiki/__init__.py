"""LLM Wiki：把原始文档在摄入时编译成结构化页面，查询时直接读页面。

和旧 RAG 的区别不在实现，在**取舍发生的时间点**：
RAG 每次查询用向量猜哪些块相关，Wiki 在编译时用 LLM 把文档嚼碎重组一次。
前者把相关性裁决交给余弦相似度（在这个知识库上已被证明不可靠），
后者交给一次有全文、有上下文的阅读理解。

模块分工：
    slug / paths / frontmatter / protocol / schema / store  确定性内核，不碰网络，全部可单测
    llm / compiler                                          编译（唯一花钱的地方）
    query / lint                                            查询与检查

规范以项目根目录的 SCHEMA.md 为准，这里的代码是它的实现。
"""
