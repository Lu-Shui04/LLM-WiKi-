"""知识库检索层：切片 → 知识单元 → 绑定 → 入库 → 召回 → 证据作答。

和旧链路（app/wiki/query.py 把整页注入上下文）的区别只有一条，但它决定了一切：

    旧：检索单元 = 页面，注入内容 = 整页 Markdown（一页一千多字）
    新：检索单元 = 小节，注入内容 = 证据片段（一句原文几十字）

实测同样两份资料，旧的一次提问注入 11,676 字，新的注入几百字——差十几倍，
而**溯源反而更准**：证据片段带 char_start/char_end，能点回源文档的那一段原文，
而不是只知道「来自哪一页」。

分工照抄 FF-LLM-Wiki知识库：
    chunk.py   源文档 → 证据片段（纯代码，不带模型）
    units.py   wiki 页面 → 知识单元（纯代码）
    bind.py    知识单元 ↔ 证据（纯代码，靠页面自带的「来自《X》」标注 + 文本覆盖）
    store.py   SQLite 表与 FTS5(trigram) 索引
    index.py   指纹与重建编排
    search.py  FTS5 召回 + 2 字 LIKE 兜底 + 关系扩一跳
    answer.py  证据整理 + 引用白名单 + 证据三态

整条索引**不打一次模型**：它是从 knowledge/ 和 wiki/ 算出来的派生物，
所以删了随时重建，指纹没变就一个字节都不动。
"""
